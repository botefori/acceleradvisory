{% extends 'base.html.twig' %}

{% block body %}
    <h1>Acc_regime_prevoyance</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ acc_regime_prevoyance.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ acc_regime_prevoyance.designation }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('prevoyances_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('prevoyances_edit', { 'id': acc_regime_prevoyance.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
